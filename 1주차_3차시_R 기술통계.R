# ?۾? ?????丮 ????(?????? ȯ?濡 ???? ?ٸ??Ƿ? ��??)
Data <- read.csv("2도수분포표_Data_01.csv", fileEncoding = "euc-kr")
str(Data)

# ???հ? ?л? ???ϱ?
Mean_A <- mean(Data$A지역)
Var_A <- var(Data$A지역)

Mean_A
Var_A

min(Data$A지역)
max(Data$A지역)

Bottom= round(min(Data$A지역), digits= 0)
Bottom

Top = round(max(Data$A지역), digits= 0)
Top


# ????????ǥ ?ۼ?
table(Data$A지역)    # ???????


Boundaries <- seq(Bottom-5, Top+5, 5)
Boundaries

table(cut(Data$A지역, Boundaries))


# ???뵵??????ǥ ?ۼ?
Length <- length(Data$A지역)
Length

table(cut(Data$A지역, Boundaries)) / Length


# ???????׷? ?ۼ?
hist(Data$A지역)

hist(Data$A지역, prob= TRUE)

hist(Data$A지역, breaks= Boundaries, prob= T, 
     main= "???뵵?? ???????׷?", ylab= "???뵵??", xlab= "????")


# ???ڱ׸? ?ۼ?
boxplot(Data$RegionA)

boxplot(Data)